name: Update EliteCC Event Count

on:
  schedule:
    - cron: "*/30 * * * *" # every 30 minutes (adjust if needed)
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Fetch events and update eventcount.json (debug + robust parsing)
        shell: bash
        run: |
          set -euo pipefail

          URL="https://api.truckersmp.com/v2/vtc/81143/events/attending"
          echo "Fetching: $URL"

          RESP="$(curl -sS "$URL")"

          echo "HTTP fetch complete."
          echo "Response first 1500 chars:"
          echo "----"
          echo "$RESP" | head -c 1500
          echo
          echo "----"

          # Try multiple possible shapes:
          # - direct array
          # - { data: [...] }
          # - { events: [...] }
          # - { results: [...] }
          COUNT="$(
            echo "$RESP" | jq '
              if type=="array" then length
              elif .data then (.data | length)
              elif .events then (.events | length)
              elif .results then (.results | length)
              else 0 end
            '
          )"

          # Optional: also print detected array length candidates if any exist
          echo "Count computed: $COUNT"

          UPDATED_AT="$(date -u +"%Y-%m-%dT%H:%M:%SZ")"

          jq -n \
            --argjson n "$COUNT" \
            --arg t "$UPDATED_AT" \
            '{ eventsThisMonth: $n, updatedAt: $t, source: "events/attending" }' \
            > eventcount.json

          echo "Written eventcount.json:"
          cat eventcount.json

      - name: Commit if changed
        shell: bash
        run: |
          if git diff --quiet; then
            echo "No changes to commit (eventcount.json unchanged)."
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git add eventcount.json
          git commit -m "Update Events This Month count"
          git push
